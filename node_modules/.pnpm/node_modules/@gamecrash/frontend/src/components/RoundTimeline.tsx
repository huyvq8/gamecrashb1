import React from "react";
import type { RoundStatus } from "../types/crash";

export function RoundTimeline({ status }: { status: RoundStatus | "IDLE" }) {
  return (
    <div data-testid="round-status">
      <div className="round-status-label">Round status</div>
      <div className="round-status-value">{status}</div>
    </div>
  );
}
