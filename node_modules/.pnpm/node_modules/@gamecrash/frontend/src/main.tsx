import React from "react";
import { createRoot } from "react-dom/client";
import { CrashScreenRebuild } from "./components/crash_rebuild/CrashScreenRebuild";
import "./styles.css";

const rootNode = document.getElementById("root");
if (rootNode) {
  const root = createRoot(rootNode);
  root.render(
    <React.StrictMode>
      <CrashScreenRebuild />
    </React.StrictMode>
  );
}
